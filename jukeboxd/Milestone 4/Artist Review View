USE jukeboxd;
CREATE or REPLACE VIEW artist_review AS
SELECT 
	a.ART_ID AS id,
	a.ART_ID,
	a.ART_Name,
	a.ART_Genre,
	b.R_ID,
	b.R_Text,
	b.R_Rating,
	b.R_TimeCreated,
	b.R_NumOfLikes
FROM ARTIST a
JOIN REVIEW b
	ON a.ART_ID = b.ART_ID;
